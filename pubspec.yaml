name: flutter_localized_countries
description: Country code to name mappings for several languages
version: 2.0.1
author: Mykola Pokhylets <pohilets@gmail.com>
homepage: https://github.com/nickolas-pohilets/flutter-localized-countries

environment:
  sdk: '>=3.0.0-0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - data/
    - data/languages.json
